<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/btnSelectSound"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Notification Sound"
        app:layout_constraintEnd_toEndOf="@+id/etTime"
        app:layout_constraintStart_toStartOf="@+id/etTime"
        app:layout_constraintTop_toBottomOf="@+id/etTime" />

    <EditText
        android:id="@+id/etTime"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnSelectSound"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:hint="Enter time in seconds"
        android:inputType="numberDecimal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.239" />

    <Button
        android:id="@+id/btnPlayNotification"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/etTime"
        android:layout_marginTop="8dp"
        android:text="Play Notification"
        app:layout_constraintEnd_toEndOf="@+id/btnSelectSound"
        app:layout_constraintStart_toStartOf="@+id/btnSelectSound"
        app:layout_constraintTop_toBottomOf="@+id/btnSelectSound" />

</androidx.constraintlayout.widget.ConstraintLayout>